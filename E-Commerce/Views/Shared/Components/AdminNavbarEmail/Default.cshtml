@model IEnumerable<Email>

<a class="dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-haspopup="true">
    <span class="icon-with-child hidden-xs">
        <span class="icon icon-envelope icon-lg icon-fw"></span>
        <span class="badge badge-danger badge-above right">@Model.Count()</span>
    </span>
    <span class="visible-xs-block">
        <span class="icon icon-bell icon-lg icon-fw"></span>
        <span class="badge badge-danger pull-right">@Model.Count()</span>
        کالاها با موجودی کمتر از نقطه سفارش
    </span>
</a>
<div class="dropdown-menu dropdown-menu-right dropdown-menu-lg">
    <div class="dropdown-header">
        <h5 class="dropdown-heading">ایمیل های جدید</h5>
    </div>
    <div class="dropdown-body">
        <div class="list-group list-group-divided custom-scrollbar">
            @foreach (var item in Model)
            {
                <a class="list-group-item" href="#">
                    <div class="notification">
                        <div class="notification-media">
                            <span class="icon icon-exclamation-triangle bg-warning rounded sq-40"></span>
                        </div>
                        <div class="notification-content">
                            @*<small class="notification-timestamp">35 min</small>*@
                            <h5 style="font-size: 20px; margin-top: 7px;" class="notification-heading">@item.strEmail</h5>
                            @*<p class="notification-text">
                                    <small class="truncate">Failed to get available update data. To ensure the proper functioning of your application, update now.</small>
                                </p>*@
                        </div>
                    </div>
                </a>
            }
        </div>
    </div>
    <div class="dropdown-footer">
        <a class="dropdown-btn" asp-area="Admin" asp-controller="Email" asp-action="Index">مشاهده همه</a>
    </div>
</div>